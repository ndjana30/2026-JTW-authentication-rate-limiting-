spring.application.name=rate
spring.datasource.url=jdbc:postgresql://localhost:5432/rate
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate/JPA Settings (Optional)
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.hibernate.ddl-auto=create-drop

# Twilio configuration - leave blank and set later in application.properties or environment
twilio.account-sid=
twilio.auth-token=
twilio.phone-number=
